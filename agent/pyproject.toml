[project]
name = "agent"
version = "0.1.0"
description = "Agent tests"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "httpx>=0.28.1",
    "mini-racer>=0.14.1",
    "openai>=2.45.0",
    "pytest",
    "pytest-asyncio>=1.4.0",
]

[dependency-groups]
dev = [
    "pytest-httpx>=0.36.2",
]
