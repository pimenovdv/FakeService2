[project]
name = "backend"
version = "0.1.0"
description = "Backend tests"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.0",
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
    "pytest",
    "uvicorn>=0.51.0",
]
